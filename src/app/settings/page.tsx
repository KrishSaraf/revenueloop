import { SettingsView } from "@/components/settings/settings-view";

export const metadata = { title: "Settings — RevenueLoop" };

export default function SettingsPage() {
  return <SettingsView />;
}
