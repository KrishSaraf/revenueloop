import { ActivityView } from "@/components/activity/activity-view";

export const metadata = { title: "Activity — RevenueLoop" };

export default function ActivityPage() {
  return <ActivityView />;
}
